{
  "mcpServers": {
    "example-local-server": {
      "command": "replace-with-command",
      "args": ["replace-with-args"],
      "env": {
        "EXAMPLE_TOKEN": "${EXAMPLE_TOKEN}"
      }
    }
  }
}
