---
description: Run the repository's portable validation gate and report exact evidence.
---

1. Read `AGENTS.md` and inspect the current Git state.
2. Run `make doctor` if the environment is unfamiliar.
3. Run `make check`.
4. Report the commands, results, skipped checks, blockers, and residual risk.
