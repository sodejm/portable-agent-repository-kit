# Antigravity Repository Adapter

Treat the root `AGENTS.md` as the controlling repository contract. Load only the
relevant skill from `.agents/skills/` for the current task. Prefer `make check` for
portable validation and do not infer permission for external side effects.
